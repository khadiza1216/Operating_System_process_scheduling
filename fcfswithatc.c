#include<stdio.h>int main(){    int p[10], at[10], bt[10], wt[10], ct[10], tat[10];    int i, j, n, temp, ct_sum = 0;    float avg_wt = 0;    printf("Number of Process: ");    scanf("%d", &n);    // Initialize process numbers    for(i = 0; i < n; i++)        p[i] = i + 1;    printf("Enter Arrival Time:\n");    for(i = 0; i < n; i++)    {        printf("P%d : ", i + 1);        scanf("%d", &at[i]);    }    printf("Enter Burst Time:\n");    for(i = 0; i < n; i++)    {        printf("P%d : ", i + 1);        scanf("%d", &bt[i]);    }    // Sort according to Arrival Time (FCFS)    for(i = 0; i < n; i++)    {        for(j = i + 1; j < n; j++)        {            if(at[i] > at[j])            {                temp = at[i];                at[i] = at[j];                at[j] = temp;                temp = bt[i];                bt[i] = bt[j];                bt[j] = temp;                temp = p[i];                p[i] = p[j];                p[j] = temp;            }        }    }    // Completion Time calculation    for(i = 0; i < n; i++)    {        if(i == 0)            ct_sum = at[i] + bt[i];        else            ct_sum = ct_sum + bt[i];        ct[i] = ct_sum;    }    // Turnaround Time & Waiting Time    for(i = 0; i < n; i++)    {        tat[i] = ct[i] - at[i];        wt[i]  = tat[i] - bt[i];        avg_wt += wt[i];    }    // Output    printf("\nProcess\tAT\tBT\tCT\tTAT\tWT\n");    for(i = 0; i < n; i++)    {        printf("P%d\t%d\t%d\t%d\t%d\t%d\n",               p[i], at[i], bt[i], ct[i], tat[i], wt[i]);    }    printf("\nAverage Waiting Time = %.2f\n", avg_wt / n);    return 0;}