#include <stdio.h>int main() {    int n, i, tq;    int bt[10], bt_rem[10], wt[10], tat[10], ct[10], p[10];    int time = 0, done = 0;    float avg_wt = 0, avg_tat = 0;    printf("Number of processes: ");    scanf("%d", &n);    for(i = 0; i < n; i++) {        p[i] = i + 1;        printf("P%d Burst Time: ", p[i]);        scanf("%d", &bt[i]);        bt_rem[i] = bt[i];  // remaining burst time    }    printf("Enter Time Quantum: ");    scanf("%d", &tq);    while(done < n) {        int flag = 0;        for(i = 0; i < n; i++) {            if(bt_rem[i] > 0) {                if(bt_rem[i] > tq) {                    time += tq;                    bt_rem[i] -= tq;                } else {                    time += bt_rem[i];                    bt_rem[i] = 0;                    ct[i] = time;                    tat[i] = ct[i];      // AT = 0                    wt[i]  = tat[i] - bt[i];                    avg_wt += wt[i];                    avg_tat += tat[i];                    done++;                }                flag = 1;            }        }        if(flag == 0) break; // সব process শেষ    }    // Output    printf("\nP\tBT\tCT\tTAT\tWT\n");    for(i = 0; i < n; i++)        printf("P%d\t%d\t%d\t%d\t%d\n", p[i], bt[i], ct[i], tat[i], wt[i]);    printf("\nAverage WT = %.2f\n", avg_wt/n);    printf("Average TAT = %.2f\n", avg_tat/n);    return 0;}