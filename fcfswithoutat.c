#include <stdio.h>int main() {    int n, i;    int bt[10], wt[10], tat[10], ct[10];    float avg_wt = 0, avg_tat = 0;    int ct_sum = 0;    printf("Enter number of processes: ");    scanf("%d", &n);    // Input Burst Time    printf("Enter Burst Time for each process:\n");    for(i = 0; i < n; i++) {        printf("P%d: ", i+1);        scanf("%d", &bt[i]);    }    // First process CT = its BT    ct[0] = bt[0];    wt[0] = 0;           // First process has 0 waiting time    tat[0] = ct[0];      // Turnaround time = CT - AT, AT = 0    // For remaining processes    for(i = 1; i < n; i++) {        ct[i] = ct[i-1] + bt[i];   // Completion time = previous CT + BT        tat[i] = ct[i];            // AT=0 → TAT = CT - 0 = CT        wt[i] = tat[i] - bt[i];    // Waiting time = TAT - BT    }    // Calculate average WT and TAT    for(i = 0; i < n; i++) {        avg_wt += wt[i];        avg_tat += tat[i];    }    // Output table    printf("\nProcess\tBT\tCT\tTAT\tWT\n");    for(i = 0; i < n; i++) {        printf("P%d\t%d\t%d\t%d\t%d\n", i+1, bt[i], ct[i], tat[i], wt[i]);    }    printf("\nAverage Waiting Time = %.2f\n", avg_wt/n);    printf("Average Turnaround Time = %.2f\n", avg_tat/n);    return 0;}