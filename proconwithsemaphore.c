#include <stdio.h>#include <stdlib.h>#define BUFFER_SIZE 5 // Fixed size of the shared bufferint buffer[BUFFER_SIZE];int in = 0; // Index for producerint out = 0; // Index for consumer// Semaphoresint mutex = 1; // Controls access to the critical section (buffer)int full = 0;  // Counts the number of filled slots in the bufferint empty = BUFFER_SIZE; // Counts the number of empty slots in the buffer// wait() and signal() functions for semaphoresint wait(int s) {    while (s <= 0); // Busy-wait if semaphore value is not positive    return --s;}int signal(int s) {    return ++s;}void producer() {    int item;    printf("Enter item to produce: ");    scanf("%d", &item);    // ADDED: detect buffer overflow (no empty slots)    if (empty == 0) {        printf("Buffer OVERFLOW! Cannot produce because buffer is full.\n");        return;    }    empty = wait(empty); // Decrement empty count, wait if buffer is full    mutex = wait(mutex); // Acquire lock for critical section    buffer[in] = item;    in = (in + 1) % BUFFER_SIZE;    printf("Producer produced item %d\n", item);    mutex = signal(mutex); // Release lock    full = signal(full);   // Increment full count}void consumer() {    int item;    // ADDED: detect buffer underflow (no filled slots)    if (full == 0) {        printf("Buffer UNDERFLOW! Cannot consume because buffer is empty.\n");        return;    }    full = wait(full);   // Decrement full count, wait if buffer is empty    mutex = wait(mutex); // Acquire lock for critical section    item = buffer[out];    out = (out + 1) % BUFFER_SIZE;    printf("Consumer consumed item %d\n", item);    mutex = signal(mutex); // Release lock    empty = signal(empty); // Increment empty count}int main() {    int choice;    printf("Producer-Consumer Problem using Semaphores\n");    while (1) {        printf("\n1. Produce\n2. Consume\n3. Exit\n");        printf("Enter your choice: ");        scanf("%d", &choice);        switch (choice) {            case 1:                producer();                break;            case 2:                consumer();                break;            case 3:                exit(0);            default:                printf("Invalid choice. Please try again.\n");        }    }    return 0;}