#include <stdio.h>#include <stdlib.h>#define BUFFER_SIZE 5int buffer[BUFFER_SIZE];int in = 0, out = 0;int count = 0;  // tracks number of items in buffervoid produce() {    int item;    if (count == BUFFER_SIZE) {        printf("Buffer OVERFLOW! Cannot produce.\n");        return;    }    printf("Enter item to produce: ");    scanf("%d", &item);    buffer[in] = item;    in = (in + 1) % BUFFER_SIZE;    count++;  // increment item count    printf("Produced: %d\n", item);}void consume() {    int item;    if (count == 0) {        printf("Buffer UNDERFLOW! Cannot consume.\n");        return;    }    item = buffer[out];    out = (out + 1) % BUFFER_SIZE;    count--;  // decrement item count    printf("Consumed: %d\n", item);}int main() {    int choice;    while (1) {        printf("\n1. Produce\n2. Consume\n3. Exit\n");        printf("Enter choice: ");        scanf("%d", &choice);        switch(choice) {            case 1: produce(); break;            case 2: consume(); break;            case 3: exit(0);            default: printf("Invalid choice.\n");        }    }    return 0;}